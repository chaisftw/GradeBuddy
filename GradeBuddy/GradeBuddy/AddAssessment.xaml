<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:GradeBuddy"
             Title="Add Assessment"
             x:Class="GradeBuddy.AddAssessment">
    <ContentPage.Content>
        <Grid Padding="5"
              RowSpacing="5"
              Grid.Column="0">

            <Grid.BindingContext>
                <local:ViewModel/>
            </Grid.BindingContext>

            <Grid.RowDefinitions>
                <RowDefinition Height="9*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackLayout Spacing="10" VerticalOptions="Start">

                <Entry Placeholder="Assessment Name" Text="{Binding AssesmentNameEntry}"/>
                <Entry Placeholder="Total Marks" Text="{Binding TotalMarksEntry}"/>
                <Entry Placeholder="Weight (%)" Text="{Binding WeightEntry}"/>
                <DatePicker Date="{Binding DateEntry}"/>

                <Button Text="Add Assessment" Command="{Binding AddAssessmentCommand}"/>
            </StackLayout>
        </Grid>

    </ContentPage.Content>
</ContentPage>